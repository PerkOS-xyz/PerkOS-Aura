# ============================================================================
# PerkOS Vendor API - Environment Variables Template
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# ⚠️ NEVER commit .env.local to git!

# ============================================================================
# Firebase Configuration (REQUIRED)
# ============================================================================
# Path to your Firebase Service Account JSON file
# Get this from: Firebase Console > Project Settings > Service accounts > Generate new private key
FIREBASE_SERVICE_ACCOUNT_PATH=./service-account.json

# Alternatively, you can provide the JSON string directly:
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# ============================================================================
# Supabase Configuration (DEPRECATED - MIGRATED TO FIREBASE)
# ============================================================================
# These are no longer used by the AI service but might be needed if you still
# have other dependencies. You can likely remove them.
# NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=supabase-anon-key...
# SUPABASE_SERVICE_ROLE_KEY=supabase-service-role-key...

# ============================================================================
# PerkOS-Stack Facilitator Configuration (REQUIRED)
# ============================================================================
NEXT_PUBLIC_FACILITATOR_URL=https://stack.perkos.xyz
FACILITATOR_URL=https://stack.perkos.xyz
STACK_URL=https://stack.perkos.xyz

# ============================================================================
# x402 Payment Configuration (REQUIRED)
# ============================================================================
# Your merchant wallet address (where payments are received)
NEXT_PUBLIC_PAY_TO_ADDRESS=0x0000000000000000000000000000000000000000
PAYMENT_WALLET_ADDRESS=0x0000000000000000000000000000000000000000

# Default network for payments
NEXT_PUBLIC_NETWORK=avalanche

# Default payment price per request
NEXT_PUBLIC_PAYMENT_PRICE_USD=0.01

# ============================================================================
# AI Service Pricing (USD) - All 20 Services
# ============================================================================
# Vision & Audio Services
NEXT_PUBLIC_AI_ANALYZE_PRICE_USD=0.05
NEXT_PUBLIC_AI_GENERATE_PRICE_USD=0.15
NEXT_PUBLIC_AI_TRANSCRIBE_PRICE_USD=0.04
NEXT_PUBLIC_AI_SYNTHESIZE_PRICE_USD=0.04

# NLP Services
NEXT_PUBLIC_AI_SUMMARIZE_PRICE_USD=0.03
NEXT_PUBLIC_AI_TRANSLATE_PRICE_USD=0.03
NEXT_PUBLIC_AI_SENTIMENT_PRICE_USD=0.02
NEXT_PUBLIC_AI_MODERATE_PRICE_USD=0.01
NEXT_PUBLIC_AI_SIMPLIFY_PRICE_USD=0.02
NEXT_PUBLIC_AI_EXTRACT_PRICE_USD=0.03

# Business Tools
NEXT_PUBLIC_AI_EMAIL_GENERATE_PRICE_USD=0.02
NEXT_PUBLIC_AI_PRODUCT_DESCRIBE_PRICE_USD=0.03
NEXT_PUBLIC_AI_SEO_OPTIMIZE_PRICE_USD=0.05

# Developer Tools
NEXT_PUBLIC_AI_CODE_GENERATE_PRICE_USD=0.08
NEXT_PUBLIC_AI_CODE_REVIEW_PRICE_USD=0.05
NEXT_PUBLIC_AI_SQL_GENERATE_PRICE_USD=0.03
NEXT_PUBLIC_AI_REGEX_GENERATE_PRICE_USD=0.02
NEXT_PUBLIC_AI_DOCS_GENERATE_PRICE_USD=0.05

# Advanced Services
NEXT_PUBLIC_AI_OCR_PRICE_USD=0.04
NEXT_PUBLIC_AI_QUIZ_GENERATE_PRICE_USD=0.05

# ============================================================================
# Service URL Configuration
# ============================================================================
NEXT_PUBLIC_SERVICE_URL=http://localhost:3000
SERVICE_URL=http://localhost:3000
SELF_URL=http://localhost:3000

# ============================================================================
# Thirdweb Authentication (REQUIRED)
# ============================================================================
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=your_thirdweb_client_id
THIRDWEB_SECRET_KEY=your_thirdweb_secret_key

# ============================================================================
# Admin Configuration (Optional)
# ============================================================================
ADMIN_WALLET_ADDRESS=0x0000000000000000000000000000000000000000
NEXT_PUBLIC_ADMIN_WALLET=0x0000000000000000000000000000000000000000


# ============================================================================
# Private Key Storage (Choose ONE method - Foundry keystore is recommended)
# ============================================================================
# 
# OPTION 1: Foundry Keystore (Recommended - same as contract deployment)
#   Import: cast wallet import defaultKey --interactive
#   Then set FOUNDRY_KEYSTORE_PASSWORD below (same password used during import)
#   Uses the same keystore format as Foundry for consistency
#
# OPTION 2: macOS Keychain (macOS only)
#   Store: npm run keychain:store 0xYourPrivateKey
#   The key will be stored securely in macOS Keychain
#
# OPTION 3: Encrypted in .env (Cross-platform)
#   Set ENCRYPTION_PASSWORD and use: npm run encrypt:key 0xYourPrivateKey
#   Then set TEST_PRIVATE_KEY_ENCRYPTED below
#
# OPTION 4: Plain text in .env (Less secure, not recommended)
#   Set TEST_PRIVATE_KEY directly (only for development)
#
# The system will try Foundry keystore first, then Keychain, then encrypted, then plain text
FOUNDRY_KEYSTORE_PASSWORD=your-foundry-keystore-password
FOUNDRY_KEYSTORE_NAME=defaultKey
ENCRYPTION_PASSWORD=your-strong-encryption-password
TEST_PRIVATE_KEY_ENCRYPTED=
# TEST_PRIVATE_KEY=0xYourPrivateKey  # Only if not using other methods

# ============================================================================
# Encrypt your private key for secure storage:
#   1. Set ENCRYPTION_PASSWORD (choose a strong password)
#   2. Run: npm run encrypt:key 0xYourPrivateKey
#   3. Copy the encrypted output to TEST_PRIVATE_KEY_ENCRYPTED below
#
# The system will automatically decrypt it at runtime using ENCRYPTION_PASSWORD
# If encryption is not used, you can set TEST_PRIVATE_KEY directly (less secure)
ENCRYPTION_PASSWORD=your-strong-encryption-password
TEST_PRIVATE_KEY_ENCRYPTED=
# OR use plain text (less secure):
# TEST_PRIVATE_KEY=0xYourPrivateKey

# ============================================================================
# elizaOS Agent Configuration (Optional - Required for chat feature)
# ============================================================================
# OpenRouter API (Recommended - supports multiple models)
# Get your API key from: https://openrouter.ai/
OPENROUTER_API_KEY=sk-or-...
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_REFERER=http://localhost:3000
OPENROUTER_TITLE=Token Service API Agent

# OpenAI API (Optional - fallback if OpenRouter not available)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Model Configuration
ELIZA_MODEL=openrouter/auto
ELIZA_TEMPERATURE=0.7

# ============================================================================
# Smart Contract Deployment
# ============================================================================
# RPC URLs (optional, defaults provided)
AVALANCHE_RPC_URL=https://api.avax.network/ext/bc/C/rpc
BASE_RPC_URL=https://mainnet.base.org
CELO_RPC_URL=https://forno.celo.org

# ============================================================================
# Development
# ============================================================================
NODE_ENV=development
NEXT_PUBLIC_APP_ENV=development
